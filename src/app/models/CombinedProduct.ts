export interface CombinedProduct {
  id: string;
  name: string;
  price: number;
  makerId: number;
  order: number;
  previousCount: number;
  remainCount: number;
  restockCount: number;
}
