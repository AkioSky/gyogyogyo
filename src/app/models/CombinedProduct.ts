export interface CombinedProduct {
  id: string;
  name: string;
  price: number;
  makerId: number;
  order: number;
  previousCount: string;
  remainCount: string;
  restockCount: string;
}
